```
git init # create .git
git add . # Add all files
git commit -m "initial" # first commit
git remote add origin git@github.com:agelosc/<repo> # set origin to github.com
git remote add origin https://github.com/agelosc/repo # alternative
git push -u origin master # Push to origin
```
